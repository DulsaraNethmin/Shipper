/// Fleet — vehicles and eligibility (`Docs/07` §2).
///
/// A vehicle's capacity and licence class determine which jobs a provider may bid on. The app
/// may filter a list to what looks eligible; eligibility itself is decided server-side, and a
/// bid on an ineligible vehicle is refused there (`Docs/07` §2, `CLAUDE.md`).
///
/// Empty until M1.
library;
