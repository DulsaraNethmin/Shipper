<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Shipper</string>
	<!-- SHIP-53. Hands an incoming URL to Flutter, which turns its path and query into a route
	     for go_router to match. Without it iOS delivers the URL and nothing reads it.

	     It applies to universal links as well as to the scheme below, so the entitlement work in
	     SHIP-24 will not have to come back and turn it on. -->
	<key>FlutterDeepLinkingEnabled</key>
	<true/>
	<!-- SHIP-53 chose the verification deep link, because internal/identity/verification.go
	     deliberately left the scheme to this ticket and sends a bare code meanwhile:

	         shipper:///verify-email?token=<token>

	     Three slashes, so the host is empty and the path is what go_router matches. A custom
	     scheme needs no registered domain, no Associated Domains entitlement and no Apple
	     Developer Program membership — X-1 and X-2 block all three, and the alternative was
	     leaving "or deep-link a code" undemonstrable for the whole of the pilot build-out.

	     It is not the production form. An HTTPS universal link is, because a custom scheme can be
	     claimed by any other app on the device, and because a link in an email has to do
	     something sensible for somebody who has not installed this one. That needs SHIP-24…27 and
	     a registered domain. No Dart changes when it arrives: a link of either kind resolves to
	     the same route with the same query parameter. -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLName</key>
			<string>au.com.shipper</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>shipper</string>
			</array>
		</dict>
	</array>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>Shipper</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- SHIP-179. iOS renders this verbatim in the camera prompt, and Apple rejects both a
	     missing purpose string and a vague one. It is kept identical to
	     PermissionCopy.cameraPurpose in lib/core/permissions/permission_copy.dart, and
	     test/core/permissions/permission_copy_test.dart fails if the two drift apart.

	     There is no equivalent key for notifications — iOS writes that prompt itself. The
	     notification rationale is in-app copy shown before the system prompt, which is what
	     Docs/07 §5 asks for regardless: the ask happens after a first bid arrives, at the
	     moment its value is obvious, and not on first launch. -->
	<key>NSCameraUsageDescription</key>
	<string>Shipper uses the camera to photograph goods at pickup and delivery, as proof the job was completed. If you cannot take a photo, you can record the reason instead.</string>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneClassName</key>
					<string>UIWindowScene</string>
					<key>UISceneConfigurationName</key>
					<string>flutter</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
					<key>UISceneStoryboardFile</key>
					<string>Main</string>
				</dict>
			</array>
		</dict>
	</dict>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
