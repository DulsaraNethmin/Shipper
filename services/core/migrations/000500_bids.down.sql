-- Reverses SHIP-80.
--
-- The trigger, the three indexes and the two foreign keys are dependents of the table and go
-- with it; naming them here would only be a second list to keep in step.
DROP TABLE IF EXISTS bids;
